let x, y, circleColor;
function setup() {
	createCanvas(windowWidth, windowHeight);
	background(100);
	circleColor = color(255, 0, 0);
}


function draw() {
	// when the function completes it's animation go to the next one
}

function firstScene(speed) {
	// The first scene of the project
	
	
}